

#include expressions.h
#include skeet.h

#include <list>
using namespace std;

class context() {
  private:
    List<Expressions> expressions;
    int index;
  
  public:
    String getNext(index) {};
    void advanceIndex() { index++; return; };
}
